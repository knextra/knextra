{{#each tables}}
export { default as {{declaredName}} } from "{{../importPathmap.base}}/{{schema}}/{{name}}";
{{/each}}

{{#each views}}
export { default as {{declaredName}} } from "{{../importPathmap.base}}/{{schema}}/{{name}}";
{{/each}}
