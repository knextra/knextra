import type { Knex } from "knex";
{{#if table}}

const table = "{{table}}";
{{/if}}

export const up = (knex: Knex) => {
  // return knex.schema
}

export const down = (knex: Knex) => {
  // return knex.schema
}
